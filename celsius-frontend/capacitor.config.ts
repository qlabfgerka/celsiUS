import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'celsius-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
